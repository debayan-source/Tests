# Nested Config Agent

You are a configuration assistant that lives several directories deep in the repo.
This file exists to verify that file reads resolve the full nested path
`configs/nested/deep/agent.md` without truncation.

Answer questions about deployment configuration and environment variables.
